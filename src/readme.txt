Code for Modelling Complex Systems, Assignment 2
By Jacques Chester, 20304893

Usage:

$ ruby simulator.rb

Usage help:

$ ruby simulator.rb -h

The code has been tested with Ruby 1.8.7 on Mac OS X 10.6.

You will need to install the sqlite-ruby and aasm gems before running the code.

$ sudo gem install sqlite-ruby aasm